name: CurrentPulse Real Image Enrichment

on:
  workflow_dispatch:
    inputs:
      stream:
        description: "coverage for Current Affairs or news"
        required: true
        default: "coverage"
        type: choice
        options:
          - coverage
          - news
      limit:
        description: "Total unresolved articles to consider (1-8)"
        required: true
        default: "6"
        type: string

permissions:
  contents: read

concurrency:
  group: currentpulse-real-image-enrichment
  cancel-in-progress: false

jobs:
  enrich:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Resolve and persist reusable real images
        shell: bash
        env:
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
          STREAM: ${{ inputs.stream || 'coverage' }}
          LIMIT: ${{ inputs.limit || '6' }}
        run: |
          set -euo pipefail
          [[ "${STREAM}" == "coverage" || "${STREAM}" == "news" ]] || { echo "Invalid stream"; exit 1; }
          [[ "${LIMIT}" =~ ^[0-9]+$ ]] || { echo "Invalid limit"; exit 1; }
          (( LIMIT >= 1 && LIMIT <= 8 )) || { echo "Limit must be 1..8"; exit 1; }
          curl --silent --show-error --fail-with-body --max-time 180 \
            -H "Authorization: Bearer ${CRON_SECRET}" \
            "https://cp.vliab.workers.dev/api/backfill-images?stream=${STREAM}&limit=${LIMIT}&wait=1"
